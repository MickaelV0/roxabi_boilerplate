// TODO: implement — import @repo/ui components
// import { Avatar, AvatarFallback, AvatarImage, Badge, Card, CardContent, CardDescription, CardHeader, CardTitle } from '@repo/ui'

/**
 * Data display composition patterns.
 *
 * Renders two patterns:
 * 1. Stat card — icon, metric value, label, trend indicator
 * 2. Profile card — avatar, name, role, bio, action buttons
 *
 * Uses real @repo/ui components with realistic (but static) data.
 */
export function DataDisplay() {
  // TODO: implement
  // 1. Render stat card compositions (2-3 stat cards in a row)
  // 2. Render profile card composition
  // 3. Responsive grid layout

  return (
    <div>
      <p>DataDisplay compositions — scaffold placeholder</p>
    </div>
  )
}
