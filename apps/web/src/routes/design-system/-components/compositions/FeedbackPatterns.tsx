// TODO: implement — import @repo/ui components
// import { Badge, Button, Card, CardContent, CardHeader, CardTitle } from '@repo/ui'

/**
 * Feedback composition patterns.
 *
 * Renders two patterns:
 * 1. Toast/alert notification — success, error, warning, info variants
 * 2. Empty state — icon, heading, description, action button
 *
 * Uses real @repo/ui components with realistic (but static) data.
 */
export function FeedbackPatterns() {
  // TODO: implement
  // 1. Render toast/alert notification examples (success, error, warning, info)
  // 2. Render empty state composition
  // 3. Responsive grid layout

  return (
    <div>
      <p>FeedbackPatterns compositions — scaffold placeholder</p>
    </div>
  )
}
