// TODO: implement — import @repo/ui components
// import { Button, Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle, Input, Label, Checkbox } from '@repo/ui'

/**
 * Auth form composition patterns.
 *
 * Renders two patterns:
 * 1. Login form — email, password, remember me, submit button
 * 2. Signup form — name, email, password, confirm password, terms checkbox, submit button
 *
 * Uses real @repo/ui components with realistic (but static) data.
 * Meant for visual reference and code copying — forms are non-functional.
 */
export function AuthForms() {
  // TODO: implement
  // 1. Render login form composition using Card, Input, Label, Button, Checkbox
  // 2. Render signup form composition
  // 3. Both in a responsive grid layout

  return (
    <div>
      <p>AuthForms compositions — scaffold placeholder</p>
    </div>
  )
}
