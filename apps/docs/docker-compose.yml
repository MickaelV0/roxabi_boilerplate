services:
  docs:
    build:
      context: ../..
      dockerfile: apps/docs/Dockerfile
    ports:
      - "${DOCS_PORT:-3002}:3002"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
