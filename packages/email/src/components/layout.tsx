// TODO: implement — shared branded email layout component
// Uses @react-email/components: Html, Head, Body, Container, Section, Img, Text, Hr
// Includes: Roxabi logo, brand colors, responsive container, footer

import type { ReactNode } from 'react'

type EmailLayoutProps = {
  children: ReactNode
  locale: string
}

export function EmailLayout(_props: EmailLayoutProps) {
  // TODO: implement — branded layout with logo, footer, responsive container
  return null
}
