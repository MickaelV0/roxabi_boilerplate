// TODO: implement — Email verification template
// Uses @react-email/components for branded responsive HTML
// Rendered via React Email's render() function
// Includes: branded layout, CTA button, expiry notice, i18n via translations

import type { Translations } from '../translations/types'

type VerificationEmailProps = {
  url: string
  translations: Translations['verification']
}

export function VerificationEmail(_props: VerificationEmailProps) {
  // TODO: implement — branded verification email with CTA button
  return null
}
